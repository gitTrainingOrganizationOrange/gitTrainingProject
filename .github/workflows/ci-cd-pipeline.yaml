name: CI/CD Pipeline

env:
  PYTHON_DIR: ci_cd_training/src
on:
  pull_request:
    branches: 
      - 'master'
  push:
    branches:
      - 'ft/training_cicd_tommy'

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd ci_cd_training/src
          pip install -r requirements.txt
          pip install flake8
          pip install bandit

      - name: Execute Flake8
        id: flake8
        run: |
          echo "::group::Flake8 Linting for Python Source"
          flake8 ${{ env.PYTHON_DIR }}/ --max-line-length=100 --exclude=__pycache__ > flake8.log 2>&1 || {
            echo "Flake8 failed. Output:";
            cat flake8.log;
            exit 1;
          }
          echo "::endgroup:: # End Flake8 Linting"

      - name: Execute Bandit
        id: bandit
        run: |
          echo "::group::Bandit Security Scan for Python Source"
          if [ -f "${{ env.PYTHON_DIR }}/.bandit" ]; then
            bandit -r ${{ env.PYTHON_DIR }}/ -ll --ini ${{ env.PYTHON_DIR }}/.bandit > bandit.log 2>&1
          else
            bandit -r ${{ env.PYTHON_DIR }}/ -ll --skip B104 > bandit.log 2>&1
          fi
          BANDIT_EXIT_CODE=$?
          if [ $BANDIT_EXIT_CODE -ne 0 ]; then
            echo "Bandit failed. Output:";
            cat bandit.log;
            exit $BANDIT_EXIT_CODE;
          fi
          echo "::endgroup:: # End Bandit Security Scan"

  terraform-deploy:
    name: "Terraform Deploy - ${{ github.base_ref || github.ref_name }}"
    needs: build-test
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.0

      - name: Auth in GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      - name: Terraform Init
        run: |
          cd ci_cd_training/terraform
          terraform init

      - name: Terraform Plan
        run: |
          cd ci_cd_training/terraform
          terraform plan -var-file=env/dev.tfvars

      - name: Terraform Apply
        run: |
          cd ci_cd_training/terraform
          terraform apply -auto-approve -var-file=env/dev.tfvars
